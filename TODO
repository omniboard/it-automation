features:
  add beter descripts of what each group installs. Not sure if it whould be in `-l` `--list` or `-h` it almost certainly should b how do we best display it.e part of `-h` but it almost certainly should be viewable in `-h`
issues:

